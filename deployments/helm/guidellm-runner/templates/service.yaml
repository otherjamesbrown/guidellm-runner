apiVersion: v1
kind: Service
metadata:
  name: {{ include "guidellm-runner.fullname" . }}
  labels:
    {{- include "guidellm-runner.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "guidellm-runner.selectorLabels" . | nindent 4 }}
